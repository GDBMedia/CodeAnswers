import { star } from 'code-answer/helpers/star';
import { module, test } from 'qunit';

module('Unit | Helper | star');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = star([42]);
  assert.ok(result);
});
