import { answerIcon } from 'code-answer/helpers/answer-icon';
import { module, test } from 'qunit';

module('Unit | Helper | answer icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answerIcon([42]);
  assert.ok(result);
});
